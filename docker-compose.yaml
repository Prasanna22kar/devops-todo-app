version: '3.8'

services:
  web:
    build: .
    container_name: todo-web
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    restart: always